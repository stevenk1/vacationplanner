// Dev stub — leaves the token empty so the app falls back to VITE_MAPBOX_TOKEN.
// In the Docker image this file is overwritten at startup with the real token from $MAPBOX_TOKEN.
window.__APP_CONFIG__ = {};
